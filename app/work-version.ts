// Visible development checkpoint. Bump this after each completed work task so
// local/device testing can confirm exactly which wrapped iteration is loaded.
export const MAYIMBE_WORK_VERSION = '0.1.1'
