node_modules
.nuxt
.output
.dist
dist
.DS_Store
.env
.env.*
!.env.example
coverage

# Local-only source/reference imports; never commit them to Mayimbe.
/Page Turner Lab/
/Page Turner Lab*.zip
/incoming/

# Local-only debugging artifacts from the on-device notebook work.
device-screen*.png
device-tiny.png
device-capture.mp4
cdp-shot*.png
canvas-debug-*.png
device-screenshots/
frames/
grid/
test-results/
visual-artifacts/

# Build-time baked notebook textures (produced by scripts/bake-notebook-textures.mjs).
public/notebook-baked
# Same encoder, written in PNG for the bake-vs-live visual regression suite.
tests/visual/.bake-png
.nuxt-layout-diag
.nuxt-visual
.nuxt-*

# Local Warp scratch directory; never commit.
.warp/

# Build-generated archive (regenerated by the build/pipeline); never commit.
generated/archive.json
